{
  "name": "ravn-test-automation-live-challenge",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "login-challenge": "npx playwright test -g 'Login using valid credentials - Challenge' --headed",
    "bad-login-challenge": "npx playwright test -g 'Login attempt using invalid credentials - Challenge' --headed",
    "place-order-challenge": "npx playwright test -g 'Place an order - Challenge' --headed"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@types/node": "^22.10.2"
  }
}
